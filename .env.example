# Environment Configuration
# For development:
NODE_ENV=development
API_URL=http://localhost:3001
PORT=3001
VITE_API_URL=http://localhost:3001

# For production:
# NODE_ENV=production
# API_URL=https://yourdomain.com  # Your actual domain
# PORT=3001
# VITE_API_URL should NOT be set in production (frontend uses same origin)

# Required for all environments
BETTER_AUTH_SECRET=
DATABASE_URL=

# CORS Configuration (Development only - production uses same-origin)
# Comma-separated list of additional allowed origins for development
# CORS_ORIGINS=http://localhost:3000,https://preview.example.com
